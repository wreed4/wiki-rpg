# Railway Environment Variables Example
# Copy these to Railway's environment variable settings

# Required: Gemini AI API Key
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Google Cloud Project (if using Imagen)
GOOGLE_CLOUD_PROJECT=your_project_id

# Database (Railway PostgreSQL addon provides this automatically)
DATABASE_URL=postgresql://user:password@host:port/database

# Redis (Railway Redis addon provides this automatically)
REDIS_URL=redis://user:password@host:port

# CORS Configuration for Frontend
FRONTEND_URL=https://your-frontend-railway-url.railway.app

# Frontend API URL (for the React app)
REACT_APP_API_URL=https://your-backend-railway-url.railway.app